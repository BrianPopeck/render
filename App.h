#pragma once

// rendering functions not associated with any particular object

#include "Ray.h"

class App {
public:
    static Vector3 L_i(const Ray& ray);

// TODO: implement indexed triangles geometry list
};
